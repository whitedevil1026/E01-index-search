# Core UI
PySide6>=6.6,<7

# Crypto / signing — already installed in user's env
cryptography>=42

# Indexing — primary. 0.22+ has TextAnalyzerBuilder; 0.25.1 adds fast-field tokenizers.
# Wheels available for cp310-cp312 on PyPI.
tantivy>=0.22,<0.26

# Encoding
charset-normalizer>=3.4,<4   # MIT — preferred over chardet 7.x

# Fuzzy hashing
python-tlsh>=4.5             # actively maintained; ssdeep optional/secondary

# Forensic stack — wheels exist on PyPI for cp310-cp312 on Windows/Linux/macOS
# If install fails on Windows, see Deps Status panel in the app for fallback steps.
libewf-python==20240506      # E01 reader; status: pre-alpha but de-facto standard
pytsk3>=20230125             # Sleuth Kit binding

# Phase 1 — image-access stack (all ship prebuilt cp310 Windows wheels,
# also bundled in ./wheels/ for offline install).
libvshadow-python>=20240504  # Volume Shadow Copy (VSS) snapshots
libbde-python>=20240502      # BitLocker decryption
libfvde-python>=20240502     # FileVault 2 decryption
libluksde-python>=20240503   # LUKS decryption
libfsntfs-python>=20240501   # direct NTFS (TSK fallback)
libfsapfs-python>=20240429   # direct APFS (TSK fallback)

# Phase 2 — content extraction (PyMuPDF + extract-msg; the rest is stdlib)
pymupdf>=1.24                # PDF text extraction
extract-msg>=0.50            # Outlook .msg parsing

# Optional (later phases):
# yara-x>=1.0
# libpff-python
